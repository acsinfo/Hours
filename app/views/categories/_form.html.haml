= simple_form_for(@category) do |f|
  = f.error_notification
  .form-inputs
    = f.input :name, required: true, autofocus: true, label: t("category.form.name")
    = f.button :submit, data: { disable_with: t("loader.saving") }
